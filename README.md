**_for development:_**

`run npm dev:client`
`run npm dev:server`

**for production**
`run npm build` - will build client side
`run npm start` - will start the server

|TODO:
Client:

- promotion card

Server:

- add cache for search
